/*eslint-env mocha*/
import expect from 'expect.js';

import {
  LayerTreeNode
} from '../index';

describe('<LayerTreeNode />', () => {

  it('is defined', () => {
    expect(LayerTreeNode).not.to.be(undefined);
  });

});
